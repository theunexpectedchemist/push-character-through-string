# push-character-through-string
Python program (using isolable subroutine) to push any character in a string to the left or right of the string
This is the branch where the actual program is worked on.
